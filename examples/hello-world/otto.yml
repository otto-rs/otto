otto:
  api: 1
  jobs: 16
  progress-interval: 10   # seconds of task silence before a "still running" line; 0 disables
  tasks:
  - punch
tasks:
  punch:
    bash: |
      echo "${arg:-donkey}"
    help: punch task help
  hello:
    params:
      -g|--greeting:
        choices:
        - howdy
        - help
        default: hello
        help: default="hello"; override greeting
    bash: |
      sleep 1
      echo "${greeting:-hello}"
    help: hello task help
  world:
    before:
    - hello
    params:
      -n|--name:
        default: world
        help: default="world"; override name
    bash: |
      echo "${name:-world}"
    help: world task help
